services:
  # User Service Database (MySQL)
  mysql-db:
    image: mysql:8.0
    container_name: mysql-db-user-service
    env_file: .env
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
      TZ: UTC
    ports:
      - "3307:3306" # Mapped to 3307 to avoid conflict with local mysql or other services
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - wds-network
    restart: unless-stopped
    command: --default-authentication-plugin=mysql_native_password

  # User Service Application
  user-service:
    build: .
    container_name: user-service
    env_file: .env
    environment:
      - SPRING_DATASOURCE_URL=jdbc:mysql://mysql-db:3306/${MYSQL_DATABASE}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
      - SPRING_DATASOURCE_USERNAME=${MYSQL_USER}
      - SPRING_DATASOURCE_PASSWORD=${MYSQL_PASSWORD}
      - JWT_SECRET=${JWT_SECRET}
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID}
      - TZ=UTC
    depends_on:
      - mysql-db
    ports:
      - "8082:8080" # Exposed on 8082 host, 8080 internal
    networks:
      - wds-network
    restart: unless-stopped

volumes:
  mysql_data:


networks:
  wds-network:
    external: true
    name: wds-network
